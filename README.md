<h1 align="center">🚀 TypeScript Template</h1>
<p align="center">
현대적인 Node.js 개발 환경을 위한 고성능 TypeScript 템플릿입니다.<br>
<b>SWC</b>를 통한 빠른 개발 속도와 <b>tsc-alias</b>를 이용한 깔끔한 빌드 환경을 제공합니다.
</p>

---

## 🛠 주요 특징 (Key Features)

- **Fast Development**: `@swc-node/register`를 사용하여 빌드 단계 없이 즉시 실행.
- **Path Aliases**: `@/` 경로 별칭 지원 (개발, 테스트, 빌드 환경 모두 완벽 적용).
- **Production Ready**: `tsc-alias`를 통해 빌드 시 경로 별칭을 실제 상대 경로로 자동 변환하여 런타임 의존성 제거.
- **Testing**: `@swc/jest` 기반의 초고속 유닛 테스트 환경 구성.
- **Code Quality**: `ESLint 9` (Flat Config) 및 `Prettier` 설정을 통한 코드 컨벤션 자동화.
- **Docker Optimized**: `멀티 스테이지 빌드`에 최적화되어 가볍고 보안성이 높은 이미지 생성 가능.

---

## 🏃 시작하기 (Getting Started)

### 1. 환경 변수 설정

본 프로젝트는 환경 변수 관리를 위해 `.env` 파일을 사용합니다.<br>
`.env.example` 파일을 복사하여 `.env` 파일을 생성한 후 필요한 값을 입력하세요.

```bash
cp .env.example .env
```

### 2. 의존성 설치

```bash
# pnpm 사용 권장
pnpm install

# 또는 npm
npm install
```

### 3. 개발 모드 실행

```bash
npm run dev

# 파일 수정 시 자동으로 서버가 재시작
npm run nodemon
```

---

## 📦 스크립트 명령어 (Scripts)

```bash
# SWC를 통해 TS 파일을 즉시 실행 (경로 별칭 지원)
npm run dev

# dist 폴더 초기화 후 컴파일 및 경로 별칭 변환 수행
npm run build

# 빌드된 프로덕션 코드(.js) 실행
npm run start

# Jest를 이용한 유닛 테스트 실행
npm run test

# Lint 및 Prettier를 이용한 코드 자동 교정
npm run fix

# 소스 변경 감지 시 자동으로 dev 스크립트 재실행
npm run nodemon
```

---

## 📁 프로젝트 구조 (Project Structure)

```text
.
├── src/                # 소스 코드 원본
│   └── index.ts        # 엔트리 포인트
├── tests/              # 테스트 파일 (*.test.ts)
├── dist/               # 빌드 결과물 (npm run build 시 생성)
├── eslint.config.mjs   # ESLint 9 설정 (Flat Config)
├── jest.config.js      # Jest 설정
├── tsconfig.json       # TypeScript 설정
└── .swcrc              # SWC 컴파일 설정
```

---

## 🐳 Docker 빌드 및 실행

```bash
# 이미지 빌드
docker build -t typescript-template .

# 컨테이너 실행
docker run -d --name my-app typescript-template
```

---

## 📝 라이선스

이 프로젝트는 [**MIT** 라이선스](/LICENSE)를 따릅니다.
